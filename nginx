server {
  listen 8080;
  server_name wiznwit.com;

  location / {
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   Host      $http_host;
    proxy_pass         http://|HOST_IP|;
  }
}
