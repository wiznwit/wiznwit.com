doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title Wizards & Witches
    //Spread the ♥
    link(
      rel='icon'
      href='/favicon.ico'
    )
    meta(
      name='description'
      content='wiznwit - Wizards & Witches - magically shifting paradigms'
    )
    meta(
      name='viewport'
      content='width=device-width, initial-scale=1'
    )
    link(
      rel='stylesheet'
      href='/css/main.css'
    )

  body(id='♥')
    section#hero
      block content
        div no content

    script.
      var d = document;
      var b = d.body;
      var s = d.createElement('script');
      b.className = b.className.replace('nojs', 'js');
      s.src = '/js/index.js';
      b.appendChild(s);
